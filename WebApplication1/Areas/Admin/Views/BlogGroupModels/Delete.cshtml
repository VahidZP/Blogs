@model WebApplication1.Models.Blogs.BlogGroupModel

@{
    ViewData["Title"] = Model.Name;
}

<div class="container mt-5">
    <h1>Delete</h1>

    <h3>Are you sure you want to delete this?</h3>
    <div>
        <h4 class="text-info">@Model.Name</h4>
        <hr />
        <dl class="row tm-bg-primary-dark tm-block tm-block-h-auto">
            <dt class = "col-sm-2">
                @Html.DisplayNameFor(model => model.Name)
            </dt>
            <dd class = "col-sm-10">
                @Html.DisplayFor(model => model.Name)
            </dd>
            <dt class = "col-sm-2">
                @Html.DisplayNameFor(model => model.UniqeUrl)
            </dt>
            <dd class = "col-sm-10">
                @Html.DisplayFor(model => model.UniqeUrl)
            </dd>
            <dt class = "col-sm-2">
                @Html.DisplayNameFor(model => model.CreateDate)
            </dt>
            <dd class = "col-sm-10">
                @Html.DisplayFor(model => model.CreateDate)
            </dd>
        </dl>
    
        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" />
            <input type="submit" value="Delete" class="btn btn-danger  text-uppercase" /> |
            <a asp-action="Index" class="btn btn-info  text-uppercase">Back to List</a>
        </form>
    </div>
</div>

